# 프로젝트 1 — 자동화 도구 비교 분석 보고서

**과제** 노코드 자동화 기초: 워크플로우 설계
**작성자** 이대흥
**작성일** 2026-08-03

---

## 1. 사용한 도구

| 도구 | 버전·플랜 | 역할 |
|---|---|---|
| **Make** (Integromat) | Free 플랜 | 기준 구현 |
| **Zapier** | Professional 14일 체험 (2026-08-03 ~ 08-17) | 동일 구조 재현 |

연동 서비스는 두 도구 모두 **Google Sheets + Gmail**로 통일했다. 과제 권장 구성(Google Sheets + Gmail/Email)에 해당하며, 두 도구 모두 무료 커넥터로 제공한다.

---

## 2. 구현한 워크플로우

### 2-1. 시나리오

**NUON 솔루션 도입 문의 접수 처리** — 문의가 들어오면 유형에 따라 다른 시트에 분류·기록하고, 담당자에게 알림 메일을 보낸다.

### 2-2. 공통 사양

| 구성 | 내용 | 요건 |
|---|---|---|
| Trigger | Google Forms 응답 제출 — 응답 시트에 신규 행 추가 | 1개 이상 ✓ |
| 조건 분기 | 폼의 `문의 유형` 필드 값으로 2갈래 | 1개 이상 ✓ |
| Action 1·2 | `솔루션 도입 상담` → **도입상담** 탭 기록 → 알림 메일 | 2개 이상 ✓ |
| Action 3·4 | `단순 문의` → **일반문의** 탭 기록 → 알림 메일 | (실제 4개) |

### 2-3. 공통 자산

- 폼: `NUON 도입 문의 (자동화 테스트용)` — Apps Script `createNuonForm`으로 생성
- 응답 시트 탭: `설문지 응답 시트1` (A~G열)
- 결과 시트: `NUON_문의처리_결과` — 탭 2개(`도입상담`, `일반문의`), A~F열

두 도구 모두 동일한 소스·동일한 구조로 구현했고, 각 분기 경로를 1회 이상 실제로 실행해 로그를 확보했다.

### 2-4. 도구별 구조

**Make — `P1_문의접수_Make`**

```
[2. Watch New Rows]
        ↓
[4. Router]
   ├─ 필터: 문의 유형 = 솔루션 도입 상담
   │    └→ [5. Add a Row] → [7. Send an email]
   └─ 필터: 문의 유형 = 단순 문의
        └→ [6. Add a Row] → [8. Send an email]
```

Router를 놓고 **경로마다 필터 조건을 따로 건다.** 경로가 먼저 있고 조건이 나중에 붙는다.

**Zapier — `P1_문의접수_Zapier`**

```
[1. New Spreadsheet Row]
        ↓
[2. Split into paths]
   ├─ 도입상담 [3. Path conditions]
   │    └→ [4. Create Row] → [5. Send Email]
   └─ 단순문의 [6. Path conditions]
        └→ [7. Create Row] → [8. Send Email]
```

Paths는 **조건과 액션이 하나의 경로 블록 안에 묶여** 있다. 경로를 만들면 조건 입력란이 이미 그 안에 들어 있다.

---

## 3. 구현 과정 요약

Make를 기준 구현으로 삼고 동일 구조를 Zapier로 옮겼다. 이 순서로 잡은 이유는 **무료 플랜에서 완주 가능한 쪽을 먼저 확정**하기 위해서였다.

| 단계 | 작업 | Make | Zapier |
|---|---|---|---|
| 01 | 준비 | Apps Script로 폼 생성 · 응답 시트 · 결과 시트(탭 2개) 구성 (공통) | |
| 02 | 계정·연동 | 무료 가입, 약 2분 | 가입 시 Pro 체험 14일 자동 적용, 약 2분 |
| 03 | 트리거 | Watch New Rows — 헤더 범위 `A1:G1` 수동 입력 | New Spreadsheet Row — 시트 선택만으로 헤더 인식 |
| 04 | 분기 | Router 배치 후 필터를 **나중에** 추가 | Paths 생성 시 조건 입력이 **함께 강제** |
| 05 | 액션 | 경로별 시트 기록 → 알림 메일 발송, 총 4개 (공통) | |
| 06 | 실행 | 필터 전 `Run once` → **양쪽 경로 통과 사고** → 필터 추가 후 재실행 | Publish 후 폼 제출 2건으로 자동 실행 |
| 07 | 검증 | 결과 시트 확인 중 **열 매핑 밀림을 양쪽에서 발견** (공통) | |

**과정에서 얻은 것**

두 도구 모두 **돌아가게 만드는 데는 큰 어려움이 없었다.** 계정 생성부터 첫 실행까지 각각 30분 안팎이었다. 시간이 걸린 건 구현이 아니라 **결과가 맞는지 확인하는 단계**였고, 실제로 아래 두 건의 오류가 모두 그 단계에서 드러났다.

---

## 4. 비교 항목별 분석

비교 항목은 리뷰 글이 아니라 **구현 중 마찰이나 사고가 발생한 지점**에서 도출했다. 마찰이 없었던 항목도 "차이가 없었다"는 사실 그대로 기록했고, 직접 확인하지 않은 기능은 서술에서 제외했다.

### 항목 1 — 워크플로우 표현 구조

| | Make | Zapier |
|---|---|---|
| 화면 형태 | 2차원 캔버스, 모듈 자유 배치 | 위에서 아래로 흐르는 선형 스텝 리스트 |
| 전체 조망 | 한 화면에서 분기 구조가 그림으로 보임 | 스크롤하며 순서대로 확인 |
| 스텝 번호 | 모듈별 고유 번호 (2·4·5·6·7·8) | 실행 순서대로 1~8 |

**해석** — Make는 워크플로우를 **다이어그램**으로, Zapier는 **체크리스트**로 다룬다. 이번 워크플로우는 분기 1개라 두 방식 모두 무리 없었지만, 경로가 3개 이상으로 늘면 Zapier의 세로 리스트는 전체 구조 파악이 어려워질 것으로 보인다.

---

### 항목 2 — 조건 분기 구현 방식 ★

| | Make | Zapier |
|---|---|---|
| 구성 요소 | Router(경로) + 필터(조건) 분리 | Paths 하나에 경로와 조건이 통합 |
| 조건 입력 위치 | 경로 연결선의 깔때기 아이콘 클릭 | 경로 블록 안 조건 입력란 |
| fallback 경로 | **지원** — 조건 없이 모두 통과시키는 예비 경로 | 개념 없음 |
| 조건 누락 시 | **모든 데이터가 통과** | 경로 저장 자체가 안 됨 |

**실측 — "조건 누락 시 모두 통과"가 실제로 일어났다**

필터를 걸기 전에 Make에서 `Run once`를 실행했고, Router의 양쪽 경로가 모두 통과해 같은 데이터가 두 탭에 기록됐다.

| 탭 | 기록된 행 |
|---|---|
| 도입상담 | 003 · **003(중복)** · 004 |
| 일반문의 | 002 · **003(오분류)** · 005 |

테스트003 한 건이 도입상담에 두 번, 일반문의에도 한 번 들어갔다. 문의 유형이 `솔루션 도입 상담`이므로 일반문의 탭에는 들어갈 수 없는 건이다. Zapier가 처리한 004·005는 각각 맞는 탭에 한 번씩만 기록됐다.

**해석** — Make의 "조건은 선택 사항" 설계는 **fallback 경로를 만들 수 있는 유연함**인 동시에 **조건을 깜빡하면 조용히 통과시키는 위험**이다. Zapier는 이 사고가 구조적으로 불가능하다. 같은 기능을 서로 다른 실패 가능성과 교환한 설계로 볼 수 있다.

기록은 지우지 않고 그대로 남겼다.

---

### 항목 3 — 데이터 매핑 방식 ★

| | Make | Zapier |
|---|---|---|
| 필드 표시 | `이름 (B)` — 열 문자 식별자 | 필드명 + **실제 응답 샘플값** |
| 시트 구조 파악 | 헤더 범위·열 ID 사용 여부를 직접 지정 | 시트 선택 시 자동 인식 |
| 체감 난이도 | **더 헤맴** | 상대적으로 수월 |

**Make에서 왜 밀렸나**

결과 시트 기록 모듈의 매핑 상태는 이랬다.

```
접수일시 (A)  ←  2. 타임스탬프 (A)
이름   (B)  ←  2. 이름 (B)
회사명  (C)  ←  2. 회사명 (C)
이메일  (D)  ←  2. 이메일 (D)
관심솔루션(E) ←  2. 관심 솔루션 (F)   ← 한 칸 어긋남
문의내용 (F)  ←  2. 문의 내용 (G)    ← 한 칸 어긋남
```

응답 시트에는 `문의 유형` 열이 중간에 있는데 결과 시트에는 없다. 분기 조건으로만 쓰이기 때문이다. 그래서 다섯 번째부터 한 칸씩 어긋난다.

문제는 **앞의 네 개가 우연히 A→A, B→B로 맞아떨어진다**는 점이다. "순서대로 꽂으면 되겠다"고 판단하기 쉬운 구조다. 게다가 Make는 기록 모듈에서 `Use column headers as IDs of the columns`가 `No`로 설정돼 있어 **열 위치가 ID로 쓰이고**, 화면에는 `(F)` 같은 열 문자만 표시된다. 값이 보이지 않으니 눈으로 검증할 방법이 없다.

트리거에서 헤더 범위를 `A1:G1`로 직접 입력해야 했던 것도 같은 성격이다. Make는 시트 구조 파악을 사용자에게 맡긴다.

**Zapier에서도 밀렸다**

도입상담 탭 4행(Zapier 생성)의 **이메일 칸에 `솔루션 도입 상담`이 기록됐다.** 이메일 주소가 아니라 문의 유형 값이다. Zapier 실행 상세(Z09)의 `Data out`에도 같은 값이 찍혀 있어, 시트 기록 단계가 아니라 **트리거가 응답 시트를 읽는 단계**에서 열이 밀린 것으로 확인된다.

**결론 수정**

Zapier가 **첫 구현을 쉽게** 만든 건 맞다. 하지만 **매핑 오류를 막아주지는 못했다.** 두 도구 모두 열 밀림이 발생했고, 차이는 오류의 발생 여부가 아니라 **발견하기까지 걸리는 시간**이었다.

---

### 항목 4 — 실행 로그 확인 방식 ★

| | Make | Zapier |
|---|---|---|
| 표시 위치 | 편집 캔버스 위에 겹쳐 표시 | 별도 History 페이지 |
| 실행된 경로 | 체크 + 처리 건수 `✓1` | `This path ran.` |
| 실행 안 된 경로 | `⊘0` — 기호 + 숫자 | `Path did not run as the rule conditions were not met.` |
| 데이터 확인 | 버블 클릭 → 팝업 | 스텝 클릭 → 우측 패널 고정 |
| 이력 보존 | 시나리오 단위 | 계정 단위 통합 히스토리 |

**해석** — `⊘0`은 정보 밀도가 높다. 여러 경로 상태를 한눈에 읽고 편집 화면을 벗어나지 않아도 된다. 대신 기호의 의미를 알아야 한다. Zapier의 문장 표시는 읽는 데 시간이 더 걸리고 페이지를 옮겨야 하지만, 처음 보는 사람도 왜 그 경로가 실행되지 않았는지 즉시 이해한다.

항목 3과 같은 방향의 차이다. UI 취향이 아니라 **"누구를 사용자로 상정했는가"**의 문제로 읽어야 한다.

---

### 항목 5 — 트리거 반응 속도 ★

| | Make (무료) | Zapier (Pro 체험) |
|---|---|---|
| 폼 제출 → 시트 기록 | 느림 | **빠름** |
| 원인 | 무료 플랜 최소 폴링 간격 15분 | 상위 플랜의 짧은 폴링 주기 |

**해석 — 통제되지 않은 변수**

같은 조건에서 비교한 게 아니다. 폴링 간격은 두 도구 모두 **플랜에 종속된 값**이다.

> Zapier가 빠른 도구인 게 아니라, **이번 구현에서 Zapier가 더 상위 플랜이었다.**

다만 "무료로 쓸 때 어느 쪽이 빠른가"를 묻는다면 Make 무료의 15분 간격은 확정된 제약이다(시나리오 하단 `Every 15 minutes` 토글로 확인). 속도가 중요하다면 **폴링 대신 Webhook**이 근본 해법이며, 두 도구 모두 지원한다. 이번 과제는 Google Forms를 소스로 썼기에 폴링을 사용했다.

---

### 항목 6 — 무료 플랜 가용 범위 ★

| | Make | Zapier |
|---|---|---|
| 제한의 성격 | **양적 제한** | **기능적 제한** |
| 구체적 내용 | 월 1,000 크레딧 / 활성 시나리오 2개 / 최소 15분 간격 | 단일 스텝만 허용 — 멀티스텝·Paths 불가 |
| 이번 구현 실측 | **9 / 1,000 크레딧 사용 (1%)** | Pro 체험 없이는 착수 불가 |
| 분기 워크플로우 무료 구현 | **가능** | **불가능** |

**해석** — 같은 "무료 플랜"이지만 막는 지점이 완전히 다르다.

Make는 기능을 다 열어두고 **사용량**으로 제한한다. 그래서 분기가 있는 워크플로우도 무료로 끝까지 만들 수 있었다. 디버깅을 포함해 여러 번 돌렸음에도 크레딧은 1%만 소모됐다.

Zapier는 **기능 자체를 잠근다.** 조건 분기(Paths)는 물론 액션이 2개 이상인 것부터 유료다. 과제 필수 요건인 "Action 2개 이상 + 조건 분기 1개 이상"은 **Zapier 무료 플랜에서 원리적으로 구현 불가능**하다.

---

### 항목 7 — 초기 설정 소요 (차이 없음)

| | Make | Zapier |
|---|---|---|
| 계정 생성 ~ Google 연동 ~ 첫 모듈 배치 | 약 2분 | 약 2분 |

유의미한 차이가 없었다. 두 도구 모두 Google 계정 OAuth 연동이 매끄러웠다.

**기록 이유** — 차이가 없다는 것도 결과다. "진입 장벽"은 두 도구를 가르는 축이 아니며, 실제 차이는 **연동 이후 첫 워크플로우를 완성하기까지의 과정**(항목 3·4)에서 발생한다는 걸 보여준다.

### 부록 — 실행 실패 대응

구현 과정에서 **실행 실패(error)는 양쪽 모두 발생하지 않았다.** 따라서 에러 핸들링·재실행 편의성은 실측 근거가 없어 비교 항목에서 제외했다.

다만 **실행은 성공했으나 결과가 잘못된 논리 오류는 양쪽 모두 발생**했다(항목 2·3). 자동화에서 위험한 건 멈추는 오류가 아니라 **조용히 잘못된 값을 남기는 오류**라는 게 이번 구현의 실감이다.

---

## 5. 종합 비교표

| 비교 항목 | Make | Zapier | 우위 |
|---|---|---|---|
| 워크플로우 표현 구조 | 2차원 캔버스 | 선형 스텝 리스트 | 구조 복잡도에 따라 |
| 조건 분기 구현 | Router+필터, fallback 지원 | Paths 통합, 조건 강제 | **Zapier** (안전성) |
| 데이터 매핑 | 열 문자 식별자 | 샘플값 동반 | **Zapier** (첫 구현 한정) |
| 실행 로그 확인 | 기호 `⊘0`, 캔버스 내 | 문장 설명, 별도 페이지 | **Zapier** (초심자 기준) |
| 트리거 반응 속도 | 느림 (무료 15분) | 빠름 (Pro 체험) | 플랜 차이, 도구 차이 아님 |
| 무료 플랜 가용 범위 | 분기 구현 가능 · 9/1,000 | 분기 구현 불가 | **Make** |
| 초기 설정 소요 | 약 2분 | 약 2분 | 차이 없음 |

---

## 6. 도구별 장단점

### Make

**장점**
- 무료 플랜에서 분기 워크플로우 완주 가능 (실측 9/1,000 사용)
- 2차원 캔버스로 분기 구조를 한눈에 조망
- 실행 결과를 편집 화면에서 바로 확인 — 화면 전환 불필요
- fallback 경로 지원 — 예외 처리 설계 여지

**단점**
- 필드가 열 문자로만 표시돼 값 검증이 어려움
- 헤더 범위·열 ID 사용 여부를 직접 지정해야 함
- **조건 누락 시 경고 없이 전량 통과** — 실제 사고 발생
- 무료 폴링 최소 15분

### Zapier

**장점**
- 필드 선택 시 실제 응답값을 함께 표시
- 미실행 사유를 자연어 문장으로 설명
- 조건 입력이 강제되어 누락이 구조적으로 불가
- 계정 단위 통합 실행 이력

**단점**
- **무료 플랜에서 이번 워크플로우 구현 불가**
- 체험 만료 시 분기 워크플로우가 잠김
- 선형 리스트라 분기가 늘면 조망성 저하
- 실행 로그가 별도 페이지 — 편집 중 이동 필요

### 공통 단점

**두 도구 모두 열 매핑 오류를 사전에 막아주지 못했다.** 실행은 성공으로 표시되지만 값이 잘못 들어간 상태를 도구가 알려주지 않는다는 점은 양쪽 공통이다.

---

## 7. 결론 — 어떤 상황에 적합한가

### 7-1. 두 도구의 성격

이번 구현에서 반복적으로 확인된 건 하나의 축이다.

> **Zapier는 첫 성공까지의 거리를 줄이고, Make는 열 번째 워크플로우의 효율을 높인다.**

Zapier가 샘플값을 보여주고(항목 3), 미실행 사유를 문장으로 설명하고(항목 4), 조건 입력을 강제하는(항목 2) 건 모두 같은 방향이다. **사용자가 아무것도 모른다고 가정하고 안내한다.**

Make가 열 문자만 노출하고, 기호로 압축해 표시하고, 조건을 선택 사항으로 두는 것도 일관된다. **사용자가 구조를 이해하고 있다고 가정하고, 대신 화면을 벗어나지 않게 한다.**

어느 쪽이 우월한 게 아니라, **학습 비용을 앞에 두느냐 뒤에 두느냐**의 차이다.

다만 이번 구현이 덧붙인 단서가 있다 — **안내가 친절하다고 결과가 정확해지지는 않았다.**

### 7-2. 상황별 선택 기준

| 상황 | 선택 | 이유 |
|---|---|---|
| 자동화를 처음 만들어 본다 | **Zapier** | 첫 성공 확률이 높음 (항목 3·4) |
| 운영자가 여러 명이라 실수 여지를 줄여야 한다 | **Zapier** | 조건 누락이 구조적으로 불가능 (항목 2) |
| 예산 없이 분기 워크플로우가 필요하다 | **Make** | 무료로 분기 구현 가능 (항목 6) |
| 분기·병합이 많은 복잡한 흐름 | **Make** | 2차원 캔버스의 조망성 + fallback (항목 1·2) |
| 실행 빈도가 낮고 단순한 연동 | Zapier | 유지 관리 부담이 적음 |
| 실행 횟수가 매우 많다 | **Make** | Zapier는 실행 단위 과금 부담이 큼 |

### 7-3. 이번 과제의 최종 선택 — Make

프로젝트 2는 **Make 단독**으로 진행한다. 근거는 두 가지다.

1. **비용 지속성** — Zapier Pro 체험이 2026-08-17에 만료되면 분기 워크플로우를 유지할 수 없다. 제출 이후에도 실제로 돌아가는 자동화를 남기려면 Make여야 한다. 크레딧도 1% 사용에 그쳐 여유가 충분하다.
2. **확장 여지** — 프로젝트 2는 단계가 더 많고 조건도 늘어난다. 항목 1에서 확인한 캔버스형 구조가 이 규모에서 유리하다.

**도구는 목적이 결정한다.** 첫 구현의 편의만 보면 Zapier가 나았다. 하지만 "체험 만료 후에도 살아 있어야 한다"는 조건이 붙는 순간 답이 바뀌었다. 대신 Make를 쓴다면 **조건 누락 점검을 절차로 만들어야 한다**는 것도 이번에 배웠다.

---

## 8. 제약사항 — 유료 기능 사용 근거 및 무료 대안

### 8-1. 사용한 유료 기능

**Zapier Paths 및 멀티스텝 Zap** — Professional 14일 무료 체험 (2026-08-03 ~ 08-17)

### 8-2. 불가피했던 이유

과제 필수 요건은 **Action 2개 이상 + 조건 분기 1개 이상**이다. Zapier 무료 플랜은 **단일 스텝 Zap만 허용**하며 Paths를 제공하지 않는다. 즉 Zapier로 이 요건을 충족하려면 유료 기능 사용이 **불가피**했다. 기능을 축소해 우회하는 방법도 없었다. 요건 자체가 무료 플랜의 경계 밖에 있기 때문이다.

### 8-3. 무료 대안

| 대안 | 내용 | 한계 |
|---|---|---|
| **A. Make 단독** | Make 무료 플랜은 Router·필터를 모두 제공 | 도구 비교라는 프로젝트 1의 목적을 달성 못 함 |
| **B. Zapier 분기 우회** | 단일 스텝 Zap 2개 + 시트 수식으로 사전 분류 | 분기 판단이 Zapier 밖에서 일어나 "워크플로우 내 조건 분기" 요건 미충족 |
| **C. n8n 자가호스팅** | Docker 로컬 구동 시 실행 무제한, IF/Switch 분기 제한 없음 | 초기 세팅 비용 발생 ("클라우드 SaaS vs 자가호스팅" 비교 축 추가 장점은 있음) |

### 8-4. 판단

프로젝트 1의 목적이 **도구 간 비교**이므로 대안 A는 선택할 수 없었다. 대안 B는 요건을 충족하지 못한다. 대안 C는 유효하나 과제 기간 내 세팅 비용을 감안해 채택하지 않았다.

따라서 **체험 기간 내에 Zapier 구현과 증빙 캡처를 모두 완료**하는 방식을 택했고, 체험 만료 후에도 유지되는 자동화는 Make 쪽에 남긴다.

### 8-5. 민감정보 처리

제출 캡처에 API Key·토큰·비밀번호는 포함되지 않는다. 테스트 데이터의 이름·회사명은 `테스트002`, `예시산업㈜` 등 가상값을 사용했다. 알림 메일 수신 주소는 본인 계정이다.

---

## 9. 증빙 캡처 목록 (23장)

### Make (9장)

| 번호 | 파일명 | 내용 |
|---|---|---|
| M00 | `M00_trigger_setup.png` | Watch New Rows — 응답 시트 지정, 헤더 범위 A1:G1 |
| M01 | `M01_scenario_overview.png` | 시나리오 전체 캔버스 (Every 15 minutes 토글 포함) |
| M02 | `M02_sheet_module.png` | 기록 모듈 — 열 헤더 ID 사용 `No` |
| M03 | `M03_data_mapping.png` | Values in columns — E·F열 어긋남 확인 |
| M04 | `M04_filter_consult.png` | 필터 — 문의 유형 Equal to 솔루션 도입 상담 |
| M05 | `M05_filter_simple.png` | 필터 — 문의 유형 Equal to 단순 문의 |
| M06 | `M06_run_consult.png` | 1st `✓1` / 2nd `⊘0` |
| M07 | `M07_run_simple.png` | 1st `⊘0` / 2nd `✓1` |
| M08 | `M08_plan_credits.png` | Free · 991/1,000 남음 · 활성 시나리오 0/2 |

### Zapier (10장)

| 번호 | 파일명 | 내용 |
|---|---|---|
| Z00 | `Z00_plan_trial.png` | Pro 체험 14일 · 만료 시 멀티스텝 잠김 |
| Z01 | `Z01_empty_zap.png` | Trigger/Action 빈 상태 |
| Z02 | `Z02_trigger_setup.png` | 트리거 — 시트 선택 시 헤더 자동 인식 |
| Z03 | `Z03_paths_create.png` | Path A / Path B 생성 |
| Z04 | `Z04_paths_condition.png` | 조건 — 문의 유형 Exactly matches |
| Z05 | `Z05_data_mapping.png` | 필드에 실제 값이 함께 표시됨 |
| Z06 | `Z06_zap_overview.png` | Publish 완료 후 전체 8스텝 |
| Z07 | `Z07_run_history.png` | 12:53:31 / 12:53:32 실행 2건 |
| Z08 | `Z08_detail_simple.png` | 단순문의 `This path ran` |
| Z09 | `Z09_detail_consult.png` | 도입상담 `This path ran` |

### 결과 확인 (4장)

| 번호 | 파일명 | 내용 |
|---|---|---|
| R01 | `R01_sheet_consult.png` | 도입상담 탭 — 중복 기록 및 이메일 칸 오기록 확인 |
| R02 | `R02_sheet_simple.png` | 일반문의 탭 — 오분류 기록 확인 |
| R03 | `R03_mail_consult.png` | [도입상담] 신규 도입 상담 문의 접수 |
| R04 | `R04_mail_simple.png` | [일반문의] 일반 문의 접수 |

---

## 10. 요건 충족 확인

| 과제 요구사항 | 충족 |
|---|---|
| 실제로 동작하는 워크플로우 | ✓ 두 도구 모두 실행 로그 확보 |
| Trigger 1개 이상 | ✓ Google Sheets 신규 행 감지 |
| Action 2개 이상 | ✓ 실제 4개 (시트 기록 2 + 메일 발송 2) |
| 조건 분기 1개 이상 | ✓ Make Router+필터 / Zapier Paths |
| 각 분기 경로 1회 이상 실행 | ✓ M06·M07 / Z08·Z09 |
| 서로 다른 2개 이상 도구 | ✓ Make + Zapier |
| 동일한 워크플로우 구조 | ✓ 2-4 참조 |
| 사용한 도구 이름 | ✓ 1장 |
| 구현 과정 요약 | ✓ 3장 |
| 5개 이상 비교 항목 | ✓ 7개 (4장) |
| 각 도구의 장단점 정리 | ✓ 6장 |
| 어떤 상황에 적합한지 | ✓ 7-2 |
| 유료 기능 사용 시 근거·무료 대안 | ✓ 8장 |
| 민감정보 미노출 | ✓ 8-5 |

---

**작성 근거** 본 보고서의 모든 비교 항목은 2026년 8월 3일 두 도구를 직접 구현하며 관찰한 내용을 기준으로 작성했다. 직접 확인하지 않은 기능은 서술에서 제외했으며, 구현 중 발생한 오류는 수정하지 않고 그대로 기록했다.
